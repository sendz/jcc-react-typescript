# Sample Code React dengan TypeScript

- `react-js` adalah contoh project dengan React JavaScript
- `react-js-migrated` adalah contoh project React JavaScript yang dimigrasikan ke TypeScript
- `react-ts` adalah contoh project baru yang memakai template `typescript` sejak awal dengan perintah `npx create-react-app my-app --template typescript`